from django.urls import path
from .views import *

urlpatterns = [
    # Add your verification endpoints here
    # Example:
    # path('send/', SendVerificationView.as_view(), name='send-verification'),
    # path('verify/', VerifyView.as_view(), name='verify'),
]
