from django.urls import path
from .views import *

urlpatterns = [
    # Add your property endpoints here
    # Example:
    # path('', PropertyListView.as_view(), name='property-list'),
    # path('<int:pk>/', PropertyDetailView.as_view(), name='property-detail'),
]
